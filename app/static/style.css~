@import url(http://fonts.googleapis.com/css?family=Dosis:200);

body            { font-family: Dosis; background: #fff; }
a, h1, h2       { color: #95a5a6; }
h1, h2          { font-family: Dosis; margin: 0; }
h1              { border-bottom: 2px solid #95a5a6; }
h2              { font-size: 1.2em; }

.page           { margin: 2em auto; width: 85%;
                  padding: 0.8em; background: #ecf0f1; }
.entries        { list-style: none; margin: 0 auto; padding: 0; }
.entries li     { margin: 0.8em 1.2em; color: #34495e; }
.entries li h2  { margin-left: -1em; }
.add-entry      { font-size: 0.9em; border-bottom: 1px solid #ccc; }
.table          { display:block; top:13%; position:absolute; left:35%; }
.metanav        { text-align: right; font-size: 0.8em; padding: 0.3em;
                  margin-bottom: 1em; }
.submit         { width:100%; border: none; height: 60px; color: #95a5a6; font-size: 30px; }
.flash          { background: #CEE5F5; padding: 0.5em;
                  border: 1px solid #AACBE2; }
.error          { background: #F0D6D6; padding: 0.5em; }
